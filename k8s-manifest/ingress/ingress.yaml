apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voting-app-ingress
spec:
  rules:
  - host: voting.vote.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: voting-service
            port:
              number: 80
  - host: voting.result.com
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: result-service
            port:
              number: 80
